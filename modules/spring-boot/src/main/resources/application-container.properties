# Profile-specific overrides, active when SPRING_PROFILES_ACTIVE=container.
# Its existence is what makes profile-specific property loading observable at runtime:
# the same key resolves differently depending on which profile is active.
demo.label=spring-boot (container profile)
