# Read through PropertySourcesPlaceholderConfigurer, which is declared by hand in
# RootConfig. Boot would register that for you; here its absence is a silent failure,
# which is exactly the contrast the pair exists to make.
db.url=${DB_URL}
db.user=${DB_USER}
db.password=${DB_PASSWORD}
